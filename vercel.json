{
  "version": 2,
  "buildCommand": "prisma generate && next build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/cron/sync-orders",
      "schedule": "*/15 * * * *"
    }
  ],
  "build": {
    "env": {
      "NODE_ENV": "production"
    }
  }
} 