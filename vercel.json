{
  "version": 2,
  "buildCommand": "prisma db push --accept-data-loss && prisma generate && next build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "crons": [],
  "build": {
    "env": {
      "NODE_ENV": "production"
    }
  }
} 