{
  "version": 2,
  "buildCommand": "prisma migrate deploy && prisma generate && next build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "crons": [],
  "build": {
    "env": {
      "NODE_ENV": "production"
    }
  }
} 